# from django.urls import path, re_path
#
# from .views import JobListView, JobDetailView, JobUpdateView
#
# urlpatterns = [
#     path("jobs/", JobListView.as_view(), name="jobs"),
#     path("jobs/<int:pk>/", JobDetailView.as_view(), name="job-detail"), # use re_path to make both job and jobs work
#     path("jobs/<int:pk>/update/", JobUpdateView.as_view(), name="job-update"),
# ]
